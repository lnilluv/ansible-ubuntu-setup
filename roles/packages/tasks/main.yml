- name: Install base package set
  ansible.builtin.apt:
    update_cache: true
    name:
      - ca-certificates
      - curl
      - gnupg
      - ufw
      - fail2ban
      - unattended-upgrades
      - auditd
      - audispd-plugins
      - libpam-pwquality
    state: present
